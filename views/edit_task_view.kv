#:kivy 1.0

<EditTaskView>:
    BoxLayout:
        orientation: 'vertical'

        HeaderBox:
            orientation: 'vertical'

            TitleLabel:
                text: "Edit Task"

        BodyBox:
            orientation: 'vertical'

            TaskViewTitle:
                hint_text: 'Title'

            TaskViewDesc:
                hint_text: "Description"

            BoxLayout:
                orientation: 'horizontal'
                size_hint: (1, None)
                height: 60
                padding: (0, 11, 0, 11)

                Widget:
                
                DeleteButton:
                    text: 'Delete'
                    on_release: app.root.current = "delete_warning"

                Widget:

            Widget:

        FooterBox:
            orientation: 'horizontal'

            SaveButton:
                text: 'Save'
                pos_hint: {'center_x': 0.5, 'center_y': 0.5}
                on_release: app.root.current = "master_list"

            Widget:

            Button:
                text: 'Cancel'
                pos_hint: {'center_x': 0.5, 'center_y': 0.5}
                on_release: app.root.current = "master_list"
        